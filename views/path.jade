h2 path - #{path.name}
a(href='/paths/#{path._id}/edit')= 'edit'

h3 collaborators
=path.collaborators

h3 dates
=path.start 
=' to '
=path.end

h3 photos
ul
	each photo in path.photos
		li
			img(src=photo.images.standard_resolution.url)